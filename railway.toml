[build]
builder = "Dockerfile"
dockerfilePath = "./kitchen-solution-sln/CustomerBackend/Dockerfile"
buildPath = "./kitchen-solution-sln/CustomerBackend"

[deploy]
start = "dotnet CustomerBackend.dll"
restartPolicyType = "never"
